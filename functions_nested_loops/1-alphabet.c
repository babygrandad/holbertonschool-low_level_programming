#include "main."

/**
  * print_alphabet - prints out the alhpabet in lowercase
  * Return: return 0 always
  */

void print_alphabet(void)
{

	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}

	_putchar('\n');
}
