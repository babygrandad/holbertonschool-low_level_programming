#include "main.h"
#include <stdio.h>

/**
 * mul - check the code
 * @a: the first interger
 * @b: the second interger
 * Return: the answer of both intergers multiplied
 */

int mul(int a, int b)
{
	return (a * b);
}
