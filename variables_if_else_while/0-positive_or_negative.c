#include <stdio.h>
#include <time.h>
/* main - function to be ran.
 * Return: (0) succsess.
 *
 */

int main (void)
{	

	int n;
	
	if(n > 0)
	{
	printf(n + " is positive");\n
	}
	else if ( n < 0)
	{
	printf(n + " is negative\n");
	}
	else	
	{ 
	printf(n +" is zero\n";
	}
}
